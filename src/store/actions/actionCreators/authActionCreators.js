import * as actionTypes from '../Actions';

export const onLogIn = data => {
    return dispatch => {
        console.log(data);
    }
}

export const onRegister = data => {
    return dispatch => {
        console.log(data);
    }
}